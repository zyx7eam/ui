---
title: '@zyxui/button | Button Component'
---

import Button from '@zyxui/button';
import Alert from '@zyxui/alert';
import { AlertCircleIcon, SparkleIcon, GithubIcon } from 'lucide-react';

# Button

The `Button` element in web development is an HTML element used to create interactive buttons that users can click or tap to trigger specific actions or functions on a web page. The `<button>` element is an essential part of web forms, user interfaces, and general interactivity.

## Basic Button

To create a basic button, simply use the Button component without passing any props. It will inherit the primary color as the default.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button>Basic</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const BasicButtonExample = () => {
    return (
      <Button>Basic</Button>
    )
  }
  ```

</Example>



## Customization

The `Button` component is <ins>**highly customizable**</ins>, allowing you to fine-tune its appearance and behavior to align with your project's unique requirements. Explore the following customization options:

### Colors

Our `Button` component offers a color palette with options such as `default`, `error`, `warning`, and `success`. To customize these default colors to align with your project's aesthetics, [`consult How to customize themes in @@zyxui/button`](#).

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button color='default'>Default</Button>
    <Button color='error'>Error</Button>
    <Button color='warning'>Warning</Button>
    <Button color='success'>Success</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithColorsExample = () => {
    return (
      <Button color='default'>Default</Button>
      <Button color='error'>Error</Button>
      <Button color='warning'>Warning</Button>
      <Button color='success'>Success</Button>
    )
  }
  ```

</Example>



### Variants

Modify `Button` styles effortlessly using the `variant` prop, with options like `solid`, `bordered`, `light`, `ghost`, `flat`, and `shadow`.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button variant='solid'>Solid</Button>
    <Button variant='bordered'>Bordered</Button>
    <Button variant='light'>Light</Button>
    <Button variant='ghost'>Ghost</Button>
    <Button variant='flat'>Flat</Button>
    <Button variant='shadow'>Shadow</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithVariants = () => {
    return (
      <Button variant='solid'>Solid</Button>
      <Button variant='bordered'>Bordered</Button>
      <Button variant='light'>Light</Button>
      <Button variant='ghost'>Ghost</Button>
      <Button variant='flat'>Flat</Button>
      <Button variant='shadow'>Shadow</Button>
    )
  }
  ```

</Example>


### Sizes

Resize your `buttons` effortlessly with the size prop. Choose from `sm` (Small), `md` (Medium), or `lg` (Large) to control the button dimensions.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button size='sm'>Small</Button>
    <Button size='md'>Medium</Button>
    <Button size='lg'>Large</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithSizes = () => {
    return (
      <Button size='sm'>Small</Button>
      <Button size='md'>Medium</Button>
      <Button size='lg'>Large</Button>
    )
  }
  ```

</Example>


### Radiuses

Easily customize the `border-radius` of your button using the `radius` prop, with options like `sm`,`md`, `lg`, `circle`, or `none`.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button radius='sm'>Small Radius</Button>
    <Button radius='md'>Medium Radius</Button>
    <Button radius='lg'>Large Radius</Button>
    <Button radius='circle'>Circle Radius</Button>
    <Button radius='none'>None Radius</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithRadiuses = () => {
    return (
      <Button radius='sm'>Small Radius</Button>
      <Button radius='md'>Medium Radius</Button>
      <Button radius='lg'>Large Radius</Button>
      <Button radius='circle'>Circle Radius</Button>
      <Button radius='none'>None Radius</Button>
    )
  }
  ```

</Example>


### Start Content

The `startContent` prop allows you to seamlessly incorporate `ReactNode` elements at the beginning of a button's child. For instance, you can employ this feature to place an `icon` at the start of a button.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button radius='sm' startContent={<GithubIcon size={18} />}>Small Radius</Button>
    <Button radius='md' startContent={<GithubIcon size={18} />}>Medium Radius</Button>
    <Button radius='lg' startContent={<GithubIcon size={18} />}>Large Radius</Button>
    <Button radius='circle' startContent={<GithubIcon size={18} />}>Circle Radius</Button>
    <Button radius='none' startContent={<GithubIcon size={18} />}>None Radius</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithStartContent = () => {
    return (
      <Button radius='sm' startContent={<GithubIcon size={18} />}>Small Radius</Button>
      <Button radius='md' startContent={<GithubIcon size={18} />}>Medium Radius</Button>
      <Button radius='lg' startContent={<GithubIcon size={18} />}>Large Radius</Button>
      <Button radius='circle' startContent={<GithubIcon size={18} />}>Circle Radius</Button>
      <Button radius='none' startContent={<GithubIcon size={18} />}>None Radius</Button>
    )
  }
  ```

</Example>


### End Content

The `endContent` prop enables the addition of `ReactNode` elements at the end of a button's child. For instance, this feature is ideal for appending an `icon` to the button's end.

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button radius='sm' endContent={<GithubIcon size={18} />}>Small Radius</Button>
    <Button radius='md' endContent={<GithubIcon size={18} />}>Medium Radius</Button>
    <Button radius='lg' endContent={<GithubIcon size={18} />}>Large Radius</Button>
    <Button radius='circle' endContent={<GithubIcon size={18} />}>Circle Radius</Button>
    <Button radius='none' endContent={<GithubIcon size={18} />}>None Radius</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithEndContent = () => {
    return (
      <Button radius='sm' endContent={<GithubIcon size={18} />}>Small Radius</Button>
      <Button radius='md' endContent={<GithubIcon size={18} />}>Medium Radius</Button>
      <Button radius='lg' endContent={<GithubIcon size={18} />}>Large Radius</Button>
      <Button radius='circle' endContent={<GithubIcon size={18} />}>Circle Radius</Button>
      <Button radius='none' endContent={<GithubIcon size={18} />}>None Radius</Button>
    )
  }
  ```

</Example>


### Start & End Content

Here are more examples to show you that [`startContent`](#start-content) & [`endContent`](#end-content) can be used together!

<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <Button radius='sm' startContent={<GithubIcon size={18} />} endContent={<GithubIcon size={18} />}>Small Radius</Button>
    <Button radius='md' startContent={<GithubIcon size={18} />} endContent={<GithubIcon size={18} />}>Medium Radius</Button>
    <Button radius='lg' startContent={<GithubIcon size={18} />} endContent={<GithubIcon size={18} />}>Large Radius</Button>
    <Button radius='circle' startContent={<GithubIcon size={18} />} endContent={<GithubIcon size={18} />}>Circle Radius</Button>
    <Button radius='none' startContent={<GithubIcon size={18} />} endContent={<GithubIcon size={18} />}>None Radius</Button>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const ButtonWithStartAndEndContent = () => {
    return (
      <Button
        radius='sm'
        startContent={<GithubIcon size={18} />}
        endContent={<GithubIcon size={18} />}
      >
        Small Radius
      </Button>
      <Button
        radius='md'
        startContent={<GithubIcon size={18} />}
        endContent={<GithubIcon size={18} />}
      >
        Medium Radius
      </Button>
      <Button
        radius='lg'
        startContent={<GithubIcon size={18} />}
        endContent={<GithubIcon size={18} />}
      >
        Large Radius
      </Button>
      <Button
        radius='circle'
        startContent={<GithubIcon size={18} />}
        endContent={<GithubIcon size={18} />}
      >
        Circle Radius
      </Button>
      <Button
        radius='none'
        startContent={<GithubIcon size={18} />}
        endContent={<GithubIcon size={18} />}
      >
        None Radius
      </Button>
    )
  }
  ```

</Example>


### Icon Only

Create icon-only buttons with ease using the `iconOnly` prop. Use icons from libraries or custom SVGs to design your unique buttons.

<Alert title='Recommendation' variant='flat' icon={<SparkleIcon size={20} />}  description={<span>It is highly recommended to leverage the extensive collection of icons available in the <strong className='text-inherit'><a href='https://lucide.dev/' target='_blank'>`lucide-react`</a></strong> icon library. This library offers a vast selection of beautifully crafted icons to enhance your design.</span>} />


<Example>
  <Preview className='flex flex-wrap flex-row gap-10 justify-center items-center'>
    <a href='https://github.com/zyx7eam/ui.git'>
      <Button iconOnly radius='sm'>
          <GithubIcon size={18} />
      </Button>
    </a>
    <a href='https://github.com/zyx7eam/ui.git'>
      <Button iconOnly radius='md'>
          <GithubIcon size={18} />
      </Button>
    </a>
    <a href='https://github.com/zyx7eam/ui.git'>
      <Button iconOnly radius='lg'>
          <GithubIcon size={18} />
      </Button>
    </a>
    <a href='https://github.com/zyx7eam/ui.git'>
      <Button iconOnly radius='circle'>
          <GithubIcon size={18} />
      </Button>
    </a>
    <a href='https://github.com/zyx7eam/ui.git'>
      <Button iconOnly radius='none'>
          <GithubIcon size={18} />
      </Button>
    </a>
  </Preview>

  ```jsx showLineNumbers
  import Button from '@zyxui/button';

  export const IconOnlyButtons = () => {
    return (
      <a href='https://github.com/zyx7eam/ui.git'>
        <Button iconOnly radius='sm'>
            <GithubIcon size={18} />
        </Button>
      </a>
      <a href='https://github.com/zyx7eam/ui.git'>
        <Button iconOnly radius='md'>
            <GithubIcon size={18} />
        </Button>
      </a>
      <a href='https://github.com/zyx7eam/ui.git'>
        <Button iconOnly radius='lg'>
            <GithubIcon size={18} />
        </Button>
      </a>
      <a href='https://github.com/zyx7eam/ui.git'>
        <Button iconOnly radius='circle'>
            <GithubIcon size={18} />
        </Button>
      </a>
      <a href='https://github.com/zyx7eam/ui.git'>
        <Button iconOnly radius='none'>
            <GithubIcon size={18} />
        </Button>
      </a>
    )
  }
  ```

</Example>


## API Reference

For an in-depth exploration of available props and advanced usage, consult the `Button` component's API reference:

| Property            | Description                               | Type        | Default     |
| --------------------|-------------------------------------------|-------------|-------------|
| className | Adding custom class names to your button. | `string`    | `undefined` |
| color | Customize button color styles. | `default` \| `error` \| `warning` \| `success` | `default` |
| variant | Change button hover and base styles. | `solid` \| `bordered` \| `light` \| `ghost` \| `flat` \| `shadow` | `solid` |
| size | Adjust button size to your specifications. | `sm` \| `md` \| `lg` | `md` |
| radius | Change button border radius style. | `sm` \| `md` \| `lg` \| `circle` \| `none` | `md` |
| iconOnly | Craft buttons with exclusive focus on icons. | `boolean` | `false` |
| startContent | Enrich buttons with content at the start [`see example`](#start-content). | `ReactNode` \| `undefined` | `undefined` |
| endContent | Elevate buttons with content at the end. [`see example`](#end-content) | `ReactNode` \| `undefined` | `undefined` |