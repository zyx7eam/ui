{
  "name": "@geeks/lib",
  "version": "0.2.3-beta",
  "main": "index.ts",
  "types": "index.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "publish-packages": "turbo run build lint test && npx changeset version && changeset publish"
  },
  "files": [
    "./*"
  ]
}
